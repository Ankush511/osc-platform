Generic single-database configuration for Open Source Contribution Platform.

To create a new migration:
    alembic revision --autogenerate -m "description of changes"

To apply migrations:
    alembic upgrade head

To downgrade:
    alembic downgrade -1
